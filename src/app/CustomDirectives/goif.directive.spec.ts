import { GoifDirective } from './goif.directive';

describe('GoifDirective', () => {
  it('should create an instance', () => {
    const directive = new GoifDirective();
    expect(directive).toBeTruthy();
  });
});
