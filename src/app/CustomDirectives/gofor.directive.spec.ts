import { GoforDirective } from './gofor.directive';

describe('GoforDirective', () => {
  it('should create an instance', () => {
    const directive = new GoforDirective();
    expect(directive).toBeTruthy();
  });
});
